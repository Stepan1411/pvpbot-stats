# 🚀 Настройка Railway Volume + GitHub Gist Backup

## Шаг 1: Создать GitHub Gist

1. Зайди на https://gist.github.com
2. Создай новый **Secret Gist**
3. Название файла: `stats_history.json`
4. Содержимое:
```json
{
  "timestamps": [],
  "servers": [],
  "bots": [],
  "spawned": [],
  "killed": []
}
```
5. Нажми **Create secret gist**
6. Скопируй **ID из URL** (например: `https://gist.github.com/username/abc123def456` → ID = `abc123def456`)

## Шаг 2: Создать GitHub Token

1. Зайди на https://github.com/settings/tokens
2. Нажми **Generate new token** → **Generate new token (classic)**
3. Название: `PVPBOT Stats Backup`
4. Выбери права: **gist** (только это!)
5. Нажми **Generate token**
6. **Скопируй токен** (он больше не появится!)

## Шаг 3: Настроить Railway Volume

1. Открой проект в Railway Dashboard
2. Перейди в **Settings** → **Volumes**
3. Нажми **New Volume**
4. Mount Path: `/data`
5. Нажми **Add**

## Шаг 4: Добавить переменные окружения

1. Перейди в **Variables**
2. Добавь переменные:

```
DATA_DIR=/data
GITHUB_GIST_TOKEN=твой_токен_из_шага_2
GITHUB_GIST_ID=твой_id_из_шага_1
```

3. Нажми **Save**

## Шаг 5: Редеплой

Railway автоматически перезапустит приложение с новыми настройками.

## ✅ Проверка

Открой в браузере:
```
https://твой-url.up.railway.app/health
```

Должно вернуться:
```json
{
  "status": "ok",
  "data_points": 0,
  "servers": 0,
  "gist_enabled": true
}
```

Если `gist_enabled: true` - всё работает! ✅

## 📊 Как это работает:

### Локальное хранение (Railway Volume):
- Данные сохраняются в `/data/stats_data.json` и `/data/stats_history.json`
- Volume **НЕ удаляется** при редеплое
- Быстрый доступ

### Резервное копирование (GitHub Gist):
- Каждые **720 точек** (примерно раз в час) → backup в Gist
- При старте Railway → загружает данные из Gist если локальных нет
- Gist **никогда не удалится**

### Восстановление:
1. Если Railway потеряет данные → автоматически загрузит из Gist
2. Если Gist пустой → начнет собирать данные заново

## 🔧 Отладка:

### Проверить логи Railway:
```
[STARTUP] Loaded 0 servers
[STARTUP] Loaded 0 history points
[STARTUP] Gist backup: enabled
```

### Если `gist_enabled: false`:
- Проверь что `GITHUB_GIST_TOKEN` и `GITHUB_GIST_ID` установлены
- Проверь что токен имеет права `gist`

### Если backup не работает:
- Проверь логи: `[BACKUP] Successfully backed up to Gist`
- Проверь что Gist существует и доступен
- Проверь что токен не истек

## 📈 Мониторинг:

### Размер данных:
- Максимум **100,000 точек** в истории
- При обновлении каждые 5 секунд = ~5.7 дней истории
- Автоматическая очистка данных старше 1 года

### Частота backup:
- Локальное сохранение: каждые 10 точек (~50 секунд)
- Gist backup: каждые 720 точек (~1 час)

## 🎯 Результат:

✅ Данные сохраняются при редеплое
✅ Автоматический backup в Gist
✅ Автоматическое восстановление
✅ Максимальная надежность
